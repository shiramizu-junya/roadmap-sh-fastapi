[project]
name = "roadmap-sh-fastapi"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.53.0",
]

[dependency-groups]
dev = [
    "debugpy>=1.8.22",
    "mypy>=2.3.1",
    "pre-commit>=4.6.2",
    "ruff>=0.16.8",
]
